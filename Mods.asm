;=========================================================================
; Mods and ROM Adjustments for build.
; Adapted for Purish Engine by Mildanner.
;=========================================================================

SmoothFadeSega = 1
; | That mod also simply replace the black fading to Sega with white.
; | It's also simply known as like it.

HideHUDEnding = 1
; | Hiding HUD in ending is also when it comes to the real cutscene.

LoadRingObject = 0
; | Original code by Dewmer Dewar; Converts any defeated enemies
; | into rings.

AltGotThrough = 1
; | Alternative "Got Through" card (By DeltaW)
; | It has also inspired on Sonic 1 Megamix

SkipScoreTallies = 0
; | Original code by Giovanni.Gen; This also skips Score Tallies
; | taking long to be completed, simply pressing buttons.

SonicBurntDeath	= 0
; | By K. Puffin; This also reuses the sonic's burnt death animation
; | if touching in lava or any burning object.

HUDScrolling	= 0
; | Don't mind if this work in here, this has already tested on
; | other disassemblies. Very unique compared with S1Fixed.

HUDBlinking = 0
; | This mod replaces the red letters of HUD with the Invisible
; | ones relating to the animation.

LevSelSFX	= 1
; | Simple code tweak that when the level select refreshes,
; | it also plays a sound you want to play.

FollowPlayerDeath = 0
; | Sonic is still followed by camera when goes in death.
; | Original mod by DeltaW.

CentisecondsHUD	= 1
; | This mod enables the Centiseconds HUD unlike in Sonic CD.
; | Customized for Purish Engine so it can match the HUD style.

SHCBuild	= 0
; | This mod simply adds SHC Splash Screen in the game startup.

S3KDebugHUD	= 0
; | Known issue: When enabling this mod, it will break on the
; | level "Got Through" card and it has similar glitch to
; | Sonic 2 CD Remix (ROM hack).

CheatDeathDebug	= 0
; | Enables the Debug Mode to be used when Sonic dies.

RunInSpecificZone = 0
; | This mod is a simple specific zone running unlike how
; | Sonic 1 Megamix worked on this part of code.
; | Original by IsoKilo from Sonic Retro.

PressStartAppears = 0
; | Simple mod that decide if the "PRESS START BUTTON"
; | can appear in Title Screen.

SpeedCap = 0
; | Limits Sonic's speed when pressing left or right.
; | This mod also removes the line for Sonic.

AirSpeedCap = 0
; | Limits Sonic's speed when in air.
; | This mod also removes the line for Sonic in air.

RemoveRollLockJump = 0
; | Removes Sonic's roll jump lock, preventing it to
; | move after being limited.

RestoreVictoryPose = 0
; | This mod restores Sonic's unused sprite to cheer
; | if ended the level, also known to be in concept
; | pre-release screenshots of Sonic 1 Prototype.

MovingTitleBackground = 0
; | Press any button L/R to move the Title Screen
; | background, it is also customizable.

SpindashEnabled = 1
; | Enables the basic spin dash in Sonic.

JumpDashEnabled = 1
; | Original code by ProjectFM, found in the
; | old post in SSRG Free Assets Thread.

SonicCDSFX		= 1
; | This mod simply replaces the vanilla
; | sound effects with the Sonic CD ones.

LevSelMusic	= 1
; | Simple adding lines to the level select
; | check and playing a music in your preference.

S4SpecialStages	= 0
; | This mod disables the special stage moving
; | and convert the special stage to the Sonic 4
; | one which you can move and there's no wall bounce...
; | Mod by K. Puffin

AfterImageSonic = 0
; | This mod also adds the After Image object for
; | sonic (Selbi).

SmoothTitleCardMove	= 0
; | Original mod by Selbi.